
<h1 style="
display: flex;
justify-content: space-between;
">Cases 
    <nav style="display: inline-block; margin-top: -8px;">
    <ul>
        <li>
            <a class="button" href="/casesUser/create">Open a New Case</a>
        </li>
    </ul>
    </nav>
</h1>

<table id="data-tables">
    <thead>
        <tr>
            <th>Description</th>
            <th>Status</th>
            <th>Link to Comments</th>
        </tr>
    </thead>
<% CasesList.forEach(function(Case) { %>
    
<tr>
    <td><%= Case.description %></td>
    <td><%= Case.status %></td>
    <!-- <td><% if (Case.requestedBy) { %> <%= Case.requestedBy.name %> <%- Case.requestedBy.personalEmail %> <% } %></td> -->
    <td>
        <!-- <% if (Case.status == 'Pending' || Case.status == 'Denied') { %>
            <a href="/Caseanization/approve/<%= Case._id%>">Approve</a>
        <% } if (Case.status == 'Pending' || Case.status == 'Approved') {%>
            <a href="/Caseanization/deny/<%= Case._id%>">Deny</a>
        <% } %>
            <a href="/Caseanization/delete/<%= Case._id%>">Delete</a> -->
            <a href="/CasesUser/Comments/<%= Case._id%>">Link to Comments</a> -->
    </td>
</tr>
<% }) %>
</table>